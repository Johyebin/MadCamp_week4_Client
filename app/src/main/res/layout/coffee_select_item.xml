<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/coffee_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:scaleType="centerCrop"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/coffee_cup" />

    <TextView
        android:id="@+id/coffee_name"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:padding="5dp"
        android:background="@android:color/white"
        android:fontFamily="@font/simple"
        android:gravity="center"
        android:text="아메리카노"
        app:layout_constraintLeft_toLeftOf="@id/coffee_image"
        app:layout_constraintRight_toRightOf="@id/coffee_image"
        app:layout_constraintTop_toBottomOf="@id/coffee_image" />

</androidx.constraintlayout.widget.ConstraintLayout>